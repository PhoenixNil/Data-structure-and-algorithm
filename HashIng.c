#include<stdio.h>
#include<stdlib.h>
#define MAXTABLESIZE 10000
typedef int ElementType;
struct Tblnode
{
    int TabSize;
    
}




int Hash(int ,int P)
{
    return %P
}





int main()
{
    int N,M,i;
    ElementType number[MAXTABLESIZE];
    HashTable H;
    scanf("%d %d",M,N); //M是表的规模，也就是T-size，N是输入的个数
    for(i=0;i<N;i++)
    {
        scanf("%d",&number[i]);
    }
    CreateTable()
    Secondaryexploration(N,)
    DestroyTable()
    return 0;
}